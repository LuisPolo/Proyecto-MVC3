@model OdeTofood.Models.RestaurantReview


    <div class="review">
        <h4>@Model.Restaurant.Name</h4>

        @Html.Image(Model.Restaurant.ImageUrl, Model.Restaurant.Name)
        <span>Rating: @Model.Rating</span>

        <p>
            @Model.Body
            <span class="right">
                @Html.ActionLink("Edit", "Edit", new {id = Model.Id})@* Puedo enviar o el nombre del controller o el id del mismo*@
            </span>
        </p>
    </div>

